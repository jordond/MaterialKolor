package com.materialkolor.sample.shared.model

/**
 * The two sections under the header.
 */
public enum class AppSection {
    /** The task list, the same in every sample. */
    Tasks,

    /** What the sample's theme module makes of the current seed and mode. Each sample fills it its own way. */
    Palette,
}
