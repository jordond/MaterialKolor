name: Deploy Builder Web App to Staging

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'builder/**'
      - 'material-kolor-core/**'
      - 'material-kolor-material3/**'
      - 'material-color-utilities/**'
      - 'gradle/**'
      - 'build-logic/**'
      - 'tools/**'
      - 'build.gradle.kts'
      - 'settings.gradle.kts'
      - 'gradle.properties'
      - '.github/workflows/builder-deploy-web.yml'

concurrency:
  group: builder-deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: "Build"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          submodules: recursive

      - name: Setup-java
        uses: actions/setup-java@v6
        with:
          java-version: 25
          distribution: temurin

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6

      - name: Build web app
        run: ./gradlew -Pmaterialkolor.useLocal=true :builder:shared:composeCompatibilityBrowserDistribution

      - name: Upload build artifacts
        uses: actions/upload-artifact@v7
        with:
          name: web-app
          path: builder/shared/build/dist/composeWebCompatibility/productionExecutable

  deploy:
    name: "Deploy to Staging"
    runs-on: ubuntu-latest
    needs:
      - build
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          submodules: recursive

      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          name: web-app
          path: builder/shared/build/dist/composeWebCompatibility/productionExecutable

      - name: Deploy to staging
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_MATERIALKOLOR }}
          channelId: staging
          projectId: materialkolor
